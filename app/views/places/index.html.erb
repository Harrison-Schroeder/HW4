<% if User.find_by({ "id" => session["user_id"] }) != nil %>

<h1>Places</h1>

<ul>
  <% for place in @places %>
    <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
  <% end %>
</ul>

<p>
  <a href="/places/new">New place</a>
</p>

<% else %>
  <p>
    You must be <a href="/login">logged in</a> to view places.
  </p>
<% end %>